<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.abelab.smartpointer.infrastructure.db.mapper.RoomMapper">

    <insert id="updatePointerTypeById">
        UPDATE `room`
        SET `pointer_type` =#{pointerType}
        WHERE `id` = #{id}
    </insert>

</mapper>
